// Generated by CoffeeScript 1.10.0
(function() {
  var FloatingButton;

  FloatingButton = (function() {
    function FloatingButton() {
      this.btn = $('#fb-div');
      this.a = $('#fb-a');
      this.btns = $('#fb-buttons');
    }

    FloatingButton.prototype.addButton = function(icon_name, color, title, handler) {
      var a, i, li;
      if (this.btn == null) {
        return;
      }
      this.a.removeClass('disabled');
      i = icon(icon_name);
      a = tag('a', "waves-effect btn-floating " + color, i);
      a.click(function() {
        handler();
        return false;
      });
      a.dynTooltip(title);
      li = tag('li', '', a);
      return this.btns.append(li);
    };

    FloatingButton.prototype.clear = function() {
      if (this.btn == null) {
        return;
      }
      (this.btns.find('tooltiped')).tooltip('remove');
      this.btns.empty();
      return this.a.addClass("disabled");
    };

    return FloatingButton;

  })();

  window.floatingButton = new FloatingButton();

}).call(this);

//# sourceMappingURL=floating_button.js.map
