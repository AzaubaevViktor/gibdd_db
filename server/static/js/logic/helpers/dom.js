// Generated by CoffeeScript 1.10.0
(function() {
  window.tag = function(name, cls, data, attrs) {
    var el;
    if (cls == null) {
      cls = "";
    }
    if (data == null) {
      data = "";
    }
    if (attrs == null) {
      attrs = {};
    }
    el = $("<" + name + " class='" + cls + "'></" + name + ">");
    el.append(data);
    el.attr(attrs);
    return el;
  };

  window.p = function(text) {
    var data;
    return tag("p", '', data = text);
  };

  window.a = function(cls, data, handler) {
    var _a;
    _a = tag('a', cls, data);
    if (typeof handler === 'function') {
      _a.on('click', function() {
        handler();
        return false;
      });
    }
    return _a;
  };

  window.div = function(cls, data, attrs) {
    if (data == null) {
      data = "";
    }
    if (attrs == null) {
      attrs = {};
    }
    return tag('div', cls, data, attrs);
  };

  window.row = function() {
    return tag('div', 'row', '');
  };

  window.icon = function(name) {
    return tag('i', 'material-icons', name);
  };

  window.inputField = function(id, type, size, label, value) {
    var d, i, l;
    if (value == null) {
      value = "";
    }
    d = div("input-field col " + size);
    i = tag('input', 'validate', '', {
      'id': id,
      'type': type,
      'value': value
    });
    l = tag('label', '', label, {
      'for': id
    });
    d.append([i, l]);
    return d;
  };

  window.valignWrapper = function(cls, innerTag) {
    innerTag.addClass('valign');
    return div("valign-wrapper " + cls, innerTag);
  };

}).call(this);

//# sourceMappingURL=dom.js.map
