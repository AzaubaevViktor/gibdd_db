// Generated by CoffeeScript 1.10.0
(function() {
  var timers;

  timers = [];

  window.delayedSendGenerator = function(timerId, params, dataGenerator, successHandler) {
    var tmhandler;
    tmhandler = function(event) {
      var _this, timer;
      progress.setColor('yellow');
      timer = timers[timerId];
      if (timer) {
        clearTimeout(timer);
      }
      _this = this;
      return timers[timerId] = setTimeout(function() {
        var data;
        data = dataGenerator(_this);
        AJAXSend(params, data, successHandler);
        return progress.setColor('green');
      }, 1000);
    };
    return tmhandler;
  };

  window.clearSendTimer = function(timerId) {
    var timer;
    timer = timers[timerId];
    if (timer) {
      return clearTimeout(timer);
    }
  };

}).call(this);

//# sourceMappingURL=delayed_send.js.map
